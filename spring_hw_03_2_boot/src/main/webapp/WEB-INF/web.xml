<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="https://jakarta.ee/xml/ns/jakartaee"
	xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
	id="WebApp_ID" version="5.0">

	<!-- The definition of the Root Spring Container shared by all Servlets 
		and Filters -->
		
	<!-- 한글 처리를 위해 encoding filter를 등록한다. -->
	<filter>
		<filter-name>encoding</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>UTF-8</param-value>
		</init-param>
	</filter>

	<filter-mapping>
		<filter-name>encoding</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>

	<!--
		404 에러 발생 시 보여줄 페이지 정의

		이 설정은 이미 완성되어 있으므로 수정할 필요 없습니다.

		동작 방식:
		- 사용자가 존재하지 않는 URL을 요청하면 404 에러가 발생합니다
		- 이 설정으로 인해 자동으로 /error/404 경로로 내부 전달됩니다
		- UserController에서 /error/404 경로를 처리하는 메서드를 작성하면 됩니다

		예시: http://localhost:8080/존재하지않는경로 요청
		     → 404 에러 발생
		     → /error/404 경로로 전달
		     → UserController의 showError404() 메서드 실행
	-->
	<error-page>
		<error-code>404</error-code>
		<location>/error/404</location>
	</error-page>
		
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>/WEB-INF/spring/root-context.xml</param-value>
	</context-param>

	<!-- Creates the Spring Container shared by all Servlets and Filters -->
	<listener>
		<listener-class>org.springframework.web.context.ContextLoaderListener
		</listener-class>
	</listener>

	<!-- Processes application requests -->
	<servlet>
		<servlet-name>appServlet</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet
		</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/spring/appServlet/servlet-context.xml
			</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>

	</servlet>

	<servlet-mapping>
		<servlet-name>appServlet</servlet-name>
		<url-pattern>/</url-pattern>
	</servlet-mapping>


</web-app>
